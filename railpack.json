{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "install": {
      "commands": ["cd backend && npm ci --omit=dev"]
    }
  },
  "deploy": {
    "startCommand": "cd backend && node src/server.js"
  }
}
